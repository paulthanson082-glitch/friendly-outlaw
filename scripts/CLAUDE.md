These scripts are operational utilities for the WritersApp project.

- `analyze-traces.sh` — builds the Swift project and runs productivity trace analysis via the WritersAppCLI `--analyze-traces` flag
- `unified_processor.py` — Python batch processor that submits JSONL data to a local analysis server
- `requirements.txt` — Python dependencies for `unified_processor.py`
